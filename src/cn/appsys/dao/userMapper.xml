<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "mybatis-3-mapper"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.appsys.dao.userMapper">
    <select id="findLogin" resultType="dev_user">
       SELECT * FROM dev_user where devCode=#{devCode}
    </select>
</mapper>